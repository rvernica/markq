# Cross-encoder reranking

A cross-encoder reranker scores (query, document) pairs jointly with full
attention, producing more accurate orderings than bi-encoder retrieval.

## Models

Qwen3-Reranker-0.6B Q8_0 is the default cross-encoder.
