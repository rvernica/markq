#!/usr/bin/env bash
# Block the commit if any Rust code isn't rustfmt-clean.
# Run `cargo fmt --all` to fix, then commit again.
# (Skip with `git commit --no-verify`.)
exec cargo fmt --all -- --check
